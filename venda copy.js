function redirectToDetails(carId) {
    // Aqui você pode adicionar o código para redirecionar para a página de detalhes do veículo
    console.log("Redirecionando para os detalhes do veículo com ID:", carId);
  }
  
  function animate(element) {
    element.style.backgroundColor = "#ff7f0e"; /* Laranja */
  }
  